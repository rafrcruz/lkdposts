# Pingflux configuration
# HOST is fixed to 127.0.0.1 by design.
PORT=3000
TRACEROUTE_DEFAULT_TARGET=8.8.8.8
TRACEROUTE_MAX_HOPS=30
TRACEROUTE_TIMEOUT_MS=10000
# Optional: override traceroute binary (useful for tests)
# TRACEROUTE_COMMAND=tracert
