name: backend-ci
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: 20.19.0
          cache: npm
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Configure env for tests
        run: |
          cat <<EOF > .env
          ${{ secrets.BACKEND_ENV }}
          EOF
          cat <<EOF > .env.test
          ${{ secrets.BACKEND_ENV }}
          EOF

      - name: Lint (silencioso)
        run: npm run lint:ci --quiet || true

      - name: Test (com cobertura se disponivel)
        run: npm run test:ci || npm test

      - name: Build
        run: npm run build

      - name: Generate OpenAPI artifact
        run: npm run docs:generate

      - name: Upload OpenAPI spec
        uses: actions/upload-artifact@v4
        with:
          name: openapi-spec
          path: backend/docs/openapi.json
          if-no-files-found: error
